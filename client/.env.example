# Absolute URL of the GraphQL endpoint, baked in at build time.
#
# Leave it empty for Vercel, the Docker image and `npm run dev` — all three
# serve the API from the app's own origin at /api/graphql, which is the
# default. Only set it when pointing the client at a server somewhere else.
VITE_API_URL=
